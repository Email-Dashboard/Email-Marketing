- body = Tilt::HamlTemplate.new { "#{@email_template.body}" }
- subject = Tilt::HamlTemplate.new { "#{@email_template.subject}" }

.content-box-large
  .panel-body
    %h2.text-center= subject.render(User.first).html_safe
    = body.render(User.first).html_safe

%hr/

= link_to 'Edit', edit_email_template_path(@email_template)
\|
= link_to 'Back', email_templates_path
