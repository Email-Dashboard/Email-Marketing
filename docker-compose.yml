version: '2'
services:
  nginx-proxy:
    image: jwilder/nginx-proxy
    container_name: nginx-proxy
    ports:
      - "80:80"
    volumes:
      - /var/run/docker.sock:/tmp/docker.sock:ro
      - /path/your/conf/my_proxy.conf:/etc/nginx/conf.d/my_proxy.conf:ro

  whoami:
    image: jwilder/whoami
    container_name: whoami
    environment:
      - VIRTUAL_HOST=whoami.local

  redis:
    image: redis:3.2-alpine
    command: redis-server --requirepass yourpassword
    ports:
      - '6379:6379'
    volumes:
      - '/datadrive/redis:/data'

  web:
    depends_on:
      - 'redis'
    image: mojilala/smart-emailing
    volumes:
      - /home/your/path/data/db:/var/db
      - /home/your/path/database.yml:/smart-email-marketing/config/database.yml
      - /your/upload/folder:/smart-email-marketing/public/upload
    environment:
      VIRTUAL_HOST: yourdomain.com
      SECRET_KEY_BASE: yourSercretKeyBase
      SIDEKIQ_USERNAME: sidekiqUsername
      SIDEKIQ_PASSWORD: sidekiqPass
      REDIS_HOST_URL: redis
      REDIS_PASSWORD: yourpassword
    links:
      - redis:redis
      - sidekiq:sidekiq

  sidekiq:
    depends_on:
      - 'redis'
    image: mojilala/smart-emailing
    restart: always
    command: 'bundle exec sidekiq -C config/sidekiq.yml -e production'
    volumes:
      - your/data/db/path:/var/db
      - /your/path/to/database.yml:/smart-email-marketing/config/database.yml
      - /your/upload/folder:/smart-email-marketing/public/upload
    environment:
      REDIS_HOST_URL: redis
      REDIS_PASSWORD: yourpassword

volumes:
  redis:
